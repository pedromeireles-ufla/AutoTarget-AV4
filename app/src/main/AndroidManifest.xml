<?xml version="1.0" encoding="utf-8"?>
<!-- Arquivo de Manifesto: Configurações principais do aplicativo Android -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoTarget">
        
        <!-- Activity Principal do Jogo -->
        <!-- android:screenOrientation="portrait" fixa a tela na vertical -->
        <!-- android:configChanges evita que a Activity reinicie ao girar a tela -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <!-- Define esta Activity como a tela inicial do aplicativo -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
